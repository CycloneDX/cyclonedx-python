[tool.poetry]
name = "normalize-packagename"
version = "0.1.0"
description = "main and dev depenndencies"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

# see https://packaging.python.org/en/latest/specifications/name-normalization/#name-normalization

[tool.poetry.dependencies]
python = "^3.8"
ruamel-yaml = "0.18.5"   # normalized from ruamel.yaml




[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
